<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "AccountEditor" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <%= javascript_include_tag "lib/html5" %>
    <![endif]-->

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">AccountEditor</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "Hilfe", Settings.help_url %></li>
              <li><%= link_to "Adressbuch", "/address_book/show" unless @user.nil? %></li>
              <li><%= link_to "Ausloggen", "/auth/logout" unless @user.nil? %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container" id="main">
        <div class="row">
          <% flash.each do |key, msg| %>
    <% msg = '<button type="button" class="close" data-dismiss="alert">×</button>'+"<strong>#{msg}</strong>" %>
    <%= content_tag :div, msg.html_safe, :id => key, :class => [:span12, :alert, "alert_#{key}"] %>
  <% end %>
        </div>
        <div class="row">
          <%= yield %>
        </div><!--/row-->

      <footer>
        <p>Piratenfraktion im Landtag NRW</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <!-- Piwik --> 
    <script type="text/javascript">
      var pkBaseURL = (("https:" == document.location.protocol) ? "https://piwik.piratenfraktion-nrw.de/" : "http://piwik.piratenfraktion-nrw.de/");
      document.write(unescape("%3Cscript src='" + pkBaseURL + "piwik.js' type='text/javascript'%3E%3C/script%3E"));
      </script><script type="text/javascript">
      try {
        var piwikTracker = Piwik.getTracker(pkBaseURL + "piwik.php", 2);
        piwikTracker.trackPageView();
        piwikTracker.enableLinkTracking();
      } catch( err ) {}
    </script><noscript><p><img src="http://piwik.piratenfraktion-nrw.de/piwik.php?idsite=2" style="border:0" alt="" /></p></noscript>
    <!-- End Piwik Tracking Code -->

  </body>
</html>
